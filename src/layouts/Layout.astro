---
import Header from "../components/Header.jsx";

export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en" class="bg-slate-100 dark:bg-slate-600 max-h-full overflow-y-scroll">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
		<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
		<link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
		<link rel="manifest" href="/site.webmanifest">
		<link rel="mask-icon" href="/safari-pinned-tab.svg" color="#5569b9">
		<meta name="msapplication-TileColor" content="#b7c3f3">
		<meta name="generator" content={Astro.generator} />
		<meta name="theme-color" content="#F1F5F9" media="(prefers-color-scheme: light)">
		<meta name="theme-color" content="#64748B" media="(prefers-color-scheme: dark)">
		<meta name="description" content={`${title} - Sofia.bio`}/>
		<title>{title} - Sofia.bio 🚀</title>
	</head>
	<body>
		<Header client:load/>
		<main class="z-0 m-auto max-w-prose dark:text-slate-300 p-2 h-full pt-32 sm:pt-44 sm:opacity-100">
			<h1 class="text-4xl font-mono font-bold mb-4 ml-1">{title}</h1>
			<slot />
		</main>
	</body>
</html>
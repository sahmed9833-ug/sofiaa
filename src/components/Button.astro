---
export interface Props {
    path: string
    title: string
}

const { path, title } = Astro.props;
---
<a href={path} class="font-mono text-paynes-grey block lg:inline-block px-4 py-2 border-solid border-2 border-periwinkle rounded-xl hover:shadow-lg">
    {title}
</a>